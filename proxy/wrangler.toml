name = "fraction-tutor-proxy"
main = "src/index.ts"
compatibility_date = "2025-09-01"

# Set the API key with:
#   npx wrangler secret put ANTHROPIC_API_KEY   (production)
# For local dev, put it in .dev.vars (gitignored):
#   ANTHROPIC_API_KEY=sk-ant-...
