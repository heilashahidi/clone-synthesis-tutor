{
  "name": "fraction-tutor-proxy",
  "version": "0.0.1",
  "private": true,
  "description": "Cloudflare Worker that forwards tutor LLM requests to Anthropic.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
